export default function EventsPage() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Events</h1>

      <p className="text-sm text-gray-600">
        UI-only placeholder. Backend will populate this later.
      </p>

      <button className="rounded-xl border border-gray-300 px-4 py-2 hover:bg-gray-50">
        See Events
      </button>
    </section>
  );
}
